# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Set a custom model (default: gpt-3.5-turbo)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Set max tokens per response (default: 500)
# MAX_TOKENS=500

# Optional: Set temperature for responses (default: 0.7)
# TEMPERATURE=0.7
